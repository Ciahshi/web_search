# web_search
A serverless Cloudflare Worker that proxies Google Search via Serper.dev API. Supports web &amp; image search with clean JSON output. No SDKs, no complex setup – just deploy and query. Perfect for search aggregators, content discovery, and backend APIs. Secure API key handling via environment variables. 🚀
